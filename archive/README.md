# Archive

These directories are no longer part of the active build:

- **api-server-deprecated/** — early Node/Express prototype, replaced by
  `artifacts/sync-backend/` (Python FastAPI). Do not reference.

- **mockup-sandbox/** — Vite + React component preview sandbox used during
  initial UI development. Not deployable; kept for reference only.
